# Roll Them Out - System Architecture

## 🏗️ System Overview Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                        GAME MANAGER                          │
│  - Score Tracking    - Time Tracking    - Game State        │
└────────────────┬────────────────────────────────────────────┘
                 │
    ┌────────────┴────────────┬──────────────┬────────────┐
    │                         │              │            │
┌───▼───────┐    ┌───────────▼──┐    ┌──────▼──────┐   │
│  PLAYER   │    │ ITEM SPAWNER │    │   ENEMY     │   │
│ CONTROLLER│    │              │    │  SPAWNER    │   │
└─────┬─────┘    └──────┬───────┘    └──────┬──────┘   │
      │                 │                    │          │
      │         ┌───────┴──────┐      ┌──────┴──────┐  │
      │         │              │      │             │  │
      │    ┌────▼────┐   ┌────▼────┐ │  ┌────────┐ │  │
      │    │ WEAPONS │   │ ARMORS  │ │  │ ENEMY  │ │  │
      │    │  ITEMS  │   │  ITEMS  │ │  │   AI   │ │  │
      │    └────┬────┘   └────┬────┘ │  └────┬───┘ │  │
      │         │             │      │       │     │  │
      └─────────┴─────────────┴──────┘       │     │  │
                     │                       │     │  │
              ┌──────▼──────┐                │     │  │
              │  ATTACHABLE │                │     │  │
              │    ITEMS    │                │     │  │
              └─────────────┘                │     │  │
                                             │     │  │
      ┌──────────────────────────────────────┘     │  │
      │                                            │  │
┌─────▼──────┐                          ┌─────────▼──▼────┐
│ PROJECTILE │◄─────────────────────────┤   CAMERA        │
│   SYSTEM   │                          │  CONTROLLER     │
└────────────┘                          └─────────────────┘
```

---

## 🔄 Core Game Loop Flow

```
   START
     │
     ▼
[Initialize Game]
     │
     ├──► [Spawn Player]
     ├──► [Setup Camera]
     ├──► [Start Spawners]
     │
     ▼
┌─────────────┐
│ GAME ACTIVE │ ◄──────────┐
└──────┬──────┘            │
       │                   │
       ▼                   │
[Update Loop] ────────────►│
       │                   │
       ├──► Player Input   │
       ├──► Physics Update │
       ├──► Item Spawning  │
       ├──► Enemy Spawning │
       ├──► Combat         │
       ├──► UI Update      │
       │                   │
       ▼                   │
  [Game Over?] ────NO──────┘
       │
      YES
       ▼
  [End Game]
```

---

## 🎯 Player Controller Flow

```
┌──────────────┐
│ PLAYER INPUT │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│  Apply Movement │
│   (Force/Torque)│
└────────┬────────┘
         │
         ▼
┌──────────────────┐
│ Detect Nearby    │
│ Items (Magnetic) │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Pull Items       │
│ (AddForce)       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Collision with   │
│ Item?            │
└────────┬─────────┘
         │
        YES
         │
         ▼
┌──────────────────┐
│ Attach Item      │
│ - Add Mass       │
│ - Create Joint   │
│ - Update COM     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Limit Speed      │
│ (Based on Mass)  │
└──────────────────┘
```

---

## 🔫 Weapon System Flow

```
┌──────────────┐
│ Weapon Item  │
│  Attached    │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Scan for Enemies │
│ (OverlapSphere)  │
└────────┬─────────┘
         │
    ┌────▼────┐
    │ Enemy   │
    │ Found?  │
    └────┬────┘
         │
        YES
         │
         ▼
┌──────────────────┐
│ Check Fire Rate  │
│  (Time Check)    │
└────────┬─────────┘
         │
       READY
         │
         ▼
┌──────────────────────┐
│ Fire Weapon          │
│ (Type-Specific)      │
│                      │
│ • Cannon             │
│ • Machine Gun        │
│ • Rocket Launcher    │
│ • Laser              │
│ • Shotgun            │
└──────────┬───────────┘
           │
           ▼
    ┌──────────────┐
    │ Apply Recoil │
    │ to Player    │
    │ (AddForceAt) │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ Spawn        │
    │ Projectile   │
    └──────────────┘
```

---

## 🛡️ Armor System Flow

```
┌──────────────┐
│ Armor Item   │
│  Attached    │
└──────┬───────┘
       │
       ├──────────┐
       │          │
       ▼          ▼
┌──────────┐  ┌──────────────┐
│ Passive  │  │   Active     │
│ Effects  │  │   Effects    │
│          │  │              │
│ • Mass   │  │ • Booster    │
│ • Defense│  │   Thrust     │
│ • Speed  │  │              │
└──────────┘  └──────┬───────┘
                     │
              ┌──────▼──────┐
              │ Every Frame │
              └──────┬──────┘
                     │
                     ▼
              ┌─────────────────┐
              │ Apply Force to  │
              │ Player (Booster)│
              └─────────────────┘
```

---

## ⚙️ Physics System Architecture

```
┌──────────────────────────────────┐
│         PLAYER RIGIDBODY         │
│                                  │
│  Properties:                     │
│  • Base Mass                     │
│  • Dynamic Center of Mass        │
│  • Velocity                      │
│  • Angular Velocity              │
└────────────┬─────────────────────┘
             │
    ┌────────┴──────────┐
    │                   │
    ▼                   ▼
┌───────────┐     ┌──────────┐
│  FORCES   │     │ TORQUES  │
└─────┬─────┘     └────┬─────┘
      │                │
      ├─► Movement     └─► Rolling
      ├─► Recoil          Effect
      ├─► Thrust
      └─► Magnetic Pull

┌──────────────────────────────────┐
│        ATTACHED ITEMS            │
│                                  │
│  Each Item Contributes:          │
│  • Mass (added to player)        │
│  • Position (affects COM)        │
│  • Fixed Joint (connection)      │
│  • Effects (weapons/armor)       │
└──────────────────────────────────┘

CENTER OF MASS CALCULATION:
═══════════════════════════════════
COM = Σ(item.position × item.mass) / Σ(mass)

Effects of COM shift:
• Leaning/tilting
• Turn bias
• Stability changes
```

---

## 🎲 Item Attachment System

```
MAGNETIC ATTRACTION PHASE
┌─────────────────────┐
│ Item Spawns         │
│ (Has Rigidbody)     │
└──────────┬──────────┘
           │
           ▼
┌──────────────────────┐
│ Player in Range?     │
│ (Distance < Magnetic │
│  Range)              │
└──────────┬───────────┘
           │
          YES
           │
           ▼
┌──────────────────────────────┐
│ Apply Force Toward Player    │
│ Force ∝ (1 - distance/range) │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────┐
│ Item Moves Closer    │
└──────────┬───────────┘
           │
           ▼
COLLISION DETECTED
           │
           ▼
ATTACHMENT PHASE
┌──────────────────────┐
│ Create Fixed Joint   │
│ Joint.connectedBody  │
│   = Player RB        │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Add Item Mass        │
│ playerMass += item   │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Set Parent           │
│ item.parent = player │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Disable Item Physics │
│ rb.isKinematic = true│
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Update Center of Mass│
│ (Recalculate)        │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Activate Item Effects│
│ (Weapons/Armor)      │
└──────────────────────┘
```

---

## 🎯 Enemy AI System

```
┌──────────────┐
│ Enemy Spawns │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Find Player      │
│ Reference        │
└──────┬───────────┘
       │
       ▼
┌─────────────────────┐
│ Every Update Frame  │
└──────┬──────────────┘
       │
       ▼
┌──────────────────────┐
│ Calculate Distance   │
│ to Player            │
└──────┬───────────────┘
       │
   ┌───▼────┐
   │ Within │
   │Detect? │
   └───┬────┘
       │
      YES
       │
   ┌───▼────────┐
   │  Within    │
   │  Attack    │
   │  Range?    │
   └───┬────────┘
       │
   ┌───┴────┐
   │        │
  YES      NO
   │        │
   ▼        ▼
┌──────┐ ┌──────────┐
│Attack│ │Move Toward│
│Player│ │  Player   │
└──────┘ └──────────┘
```

---

## 📊 Data Flow Summary

```
INPUT LAYER
     ↓
CONTROLLER LAYER (PlayerController, Enemy)
     ↓
PHYSICS LAYER (Unity Rigidbody, Joints)
     ↓
GAME LOGIC LAYER (Items, Combat)
     ↓
VISUAL/FEEDBACK LAYER (Camera, UI, Effects)
```

---

## 🔌 Component Dependencies

```
PlayerController
├── Requires: Rigidbody
├── Uses: AttachableItem (interface)
└── Affected by: WeaponItem, ArmorItem

AttachableItem (Base Class)
├── Requires: Rigidbody
└── Derived: WeaponItem, ArmorItem

WeaponItem
├── Extends: AttachableItem
├── Uses: Projectile
└── Affects: PlayerController (recoil)

ArmorItem
├── Extends: AttachableItem
└── Affects: PlayerController (thrust)

Enemy
├── Requires: Rigidbody
└── Targets: PlayerController

GameManager (Singleton)
├── References: PlayerController
├── Manages: Score, Time
└── Controls: Game State

CameraController
└── Follows: PlayerController

Spawners
├── ItemSpawner → Creates WeaponItem/ArmorItem
└── EnemySpawner → Creates Enemy
```

---

## 💡 Key System Interactions

### 1. Mass and Speed
```
Player Speed = Max Speed × (Base Mass / Current Mass)

As items attach:
Mass ↑ → Speed ↓ → Control ↑
```

### 2. Recoil Physics
```
Weapon fires → Apply force at attachment point
→ Player moves opposite direction
→ If off-center, also creates rotation
```

### 3. Center of Mass
```
COM = Weighted average of all item positions
Affects:
• Balance
• Turn radius
• Stability
```

---

This architecture creates emergent gameplay through simple physics interactions!

